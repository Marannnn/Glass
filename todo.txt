Main window
Add new - window	x

Show new window on button press x
	load file 
		-> show file manager dialog {Open file picker async}	x
		select existing prefix		X WORKING ON 
			combobox -> load saved prefixes from collection  
			save to program class
	start wine 
		wine will start it in the apps selected prefix

	add into user library
		-show name
	prefixes		
		create new ones
		save prefixes paths into a file
		
		everytime the program looks into the file and loads the name of the prefix ( for organizatinos purps) and their paths
	custom themes

Color pallete


##################

what im working on
--------------------

save prefixes into json file with arch and path
	default wine prefix save json 64bit

MainViewModel show prefixes architecture

!IMPORTANT!
prefix class - prefixPath and architecture


ON START; check if saved path stil exists before loading into ObservableCollection

AddFile show selected file

AddFIle add new instance of class
	show the instance in MainViewModel.axaml

	ON-CLICK: 
		start the file fie with the specified prefix
			+(method StartFile wine.cs)194


#######
current problem	/	current commit
---------------
StartFile with prefix						X								

wine.cs fileStart doesnt start ,path 		X

start file standalone						X

! addFile a addPrefix sync mezi  MainWindowViewmodel		-save, load,..

WineProgram 
	mainviewmodel create file if doesnt exist	X
	
	wine.cs method -> load and deserialize winePrograms, returns collection		X

	mainviewmodel displays collection content and on click starts specific program		working

	save program files into json (WineProgram class)
		detect if game was succesfuly added

open dialog if program added


Check if prefix paths actually exist



AddFile.axaml.cs AddFile removed and just used the viewmodel one

WineProgram class now uses Prefix class isntead of string
